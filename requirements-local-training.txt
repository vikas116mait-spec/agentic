# Keep the local training stack reproducible across rebuilds. These versions
# match the workspace environment used to produce recent successful Unsloth
# runs and avoid "worked yesterday, broke after rebuild" drift.
transformers==5.5.0
datasets==4.3.0
peft==0.19.1
trl==0.24.0
accelerate==1.13.0
bitsandbytes==0.49.2
python-dotenv==1.2.2
unsloth==2026.4.6
# Needed by `scripts/recover_ollama.py` and llama.cpp's `convert_hf_to_gguf.py`
# so we can re-export a trained LoRA -> GGUF and register it with Ollama when
# the Unsloth-bundled GGUF pipeline fails at training time.
gguf==0.18.0
